[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
adaptive_llama = { path = "../adaptive_llama" }
tool_belt    = { path = "../tool_belt" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tiny_http = "0.12"
